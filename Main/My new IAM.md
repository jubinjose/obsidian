

Here's a layered look at the IAM landscape, with IGA and PAM broken out in detail.

**IAM** is the umbrella discipline — it covers every mechanism for managing digital identities and controlling access. Underneath it sit four major pillars: **IAM Core** (authentication/SSO), **IGA** (governance & lifecycle), **PAM** (privileged access), and **CIAM** (customer identity). Let's map them visually, then zoom into IGA and PAM.

V

visualize

V

visualize show_widget

Now let's zoom into **IGA** and **PAM** individually with their full component breakdowns.

V

visualize

V

visualize show_widget